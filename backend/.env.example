# Database Configuration
DATABASE_URL="sqlserver://server:port;database=your_database;user=your_username;password=your_password;trustServerCertificate=true;encrypt=false;integratedSecurity=false"

# JWT Secret Key (Generate a strong secret key for production)
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# Environment
NODE_ENV="production"

# Server Port
PORT=3003

# Instructions:
# 1. Copy this file to .env
# 2. Replace all placeholder values with your actual configuration
# 3. Never commit the .env file to version control
# 4. Generate a strong JWT_SECRET using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"